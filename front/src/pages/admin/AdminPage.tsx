import * as React from 'react';
import * as styles from './adminPage.scss';

export function AdminPage() {
    return (
        <div className={styles.adminPage}>
            <span className={styles.text}>Admin Page</span>
        </div>
    );
}

export default AdminPage;